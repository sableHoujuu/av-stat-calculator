[project]
name = "av-stat-calculator"
version = "0.1.0"
description = "A full-fat stat calculator for Anime Vanguards, with user-updatable data."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.3",
]
